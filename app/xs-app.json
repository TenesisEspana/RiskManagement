
{
  "welcomeFile": "/app/risks/webapp/index.html",
  "routes": [
    {
      "source": "^/app/(.*)$",
      "localDir": "./",
      "target": "$1"
    },
    {npm update --package-lock-only
      "source": "^/service/(.*)$",
      "destination": "srv-api"
    }
  ]
}
